# Arm 2 sweep, Jetson AGX Orin -- FALLBACK environment for Qwen2.5-VL only. Build it ONLY if the validation
# gate FAILS in the main environment (requirements-jetson-arm2.txt, transformers 4.x) and the failure looks
# version-related (model-input-size check / median pixel deviation; see the doc). Same install sequence as the
# main environment, in a SECOND venv, with this file instead.
#
# transformers 5.17.0 is the version the historical local CPU reference run used (docs/
# ARM2_MINIMAL_BASELINE_MULTI_CANDIDATE.md), and was the newest transformers on PyPI on 2026-09-22
# (requires Python >= 3.10; a dry-run resolve for cp310 aarch64 with torch 2.8.0 succeeds, pulling
# huggingface_hub 1.x and tokenizers 0.23). Moondream2 cannot run here (the driver blocks it loudly), and
# timm/einops are not needed for Qwen/PaliGemma2 -- so InternVL2.5, PaliGemma2 and Moondream2 stay in the main environment.
-r requirements-jetson-arm2-common.txt

transformers==5.17.0
huggingface_hub
