# Arm 2 sweep, Jetson AGX Orin -- the transformers==5.17.0 CONTAINER image's extra packages, built by
# Dockerfile.arm2-transformers5 (this directory). Container pivot, 2026-09-22.
#
# CORRECTED 2026-09-22 (real on-device probe, this session), same finding as requirements-jetson-arm2.txt's
# header: this file's name ("fallback") and its original framing ("build ONLY if the validation gate FAILS
# in the main [transformers<5] environment") are now backwards. A real probe on this Jetson under
# transformers==4.57.6 showed `Qwen2_5_VLForConditionalGeneration` AND `PaliGemmaForConditionalGeneration`
# both MISSING from the installed transformers -- confirmed, not a hypothesis the gate needed to test. So
# this is not a conditional fallback any more: it is the REQUIRED, primary environment for Qwen2.5-VL and
# PaliGemma2 -- neither can even be imported under transformers<5, let alone gated on it. The validation
# gate (`--stage validate`, compares this container's Qwen output against the local CPU reference that also
# ran under transformers 5.17.0) still runs from here, as before -- it is just no longer conditional on the
# other container failing first.
#
# Moondream2 cannot run here (transformers>=5 breaks its remote code, docs/
# ARM2_MINIMAL_BASELINE_MULTI_CANDIDATE.md S8.4; `candidate_issues()` in run_arm2_sweep_jetson.py BLOCKS it
# here automatically, no code change needed) -- it runs from Dockerfile.arm2-transformers4 /
# requirements-jetson-arm2.txt instead, alongside InternVL2.5 (which works under either transformers major
# and is kept there rather than duplicated into both images). timm/einops are correspondingly not needed
# here: this image runs qwen2_5_vl_3b and paligemma2_3b_mix (:prompt / :detect) only.
#
# transformers 5.17.0 is the version the historical local CPU reference run used (docs/
# ARM2_MINIMAL_BASELINE_MULTI_CANDIDATE.md), and was the newest transformers on PyPI on 2026-09-22
# (requires Python >= 3.10; a dry-run resolve for cp310 aarch64 with torch 2.8.0 succeeds, pulling
# huggingface_hub 1.x and tokenizers 0.23 -- re-verify against this container's actual torch version, since
# the base image ships torch 2.4.0, not 2.8.0; if pip's resolver under `-c <base pip freeze>` backtracks or
# refuses, that is the frozen-constraints mechanism doing its job, not a bug -- see the Dockerfile).
-r requirements-jetson-arm2-common.txt

transformers==5.17.0
huggingface_hub
