# Arm 2 sweep, Jetson AGX Orin -- the version-CHANGING step, for the SECOND environment only
# (a clone of the working Qwen environment; see host_software/ml_jetson_vla/docs/JETSON_ARM2_SWEEP_LOCAL.md).
# Never install this into the original Qwen environment.
#
# Why this file exists: Moondream2's remote code (pinned revision 2025-06-21) is reported broken on
# transformers>=5 (docs/ARM2_MINIMAL_BASELINE_MULTI_CANDIDATE.md S8.4), while the dev-machine Qwen run
# used transformers 5.17.0. `arm2_colab_sweep.ipynb` resolved the same conflict with
# `transformers>=4.56,<5`; the same range is used here so Jetson and Colab runs of the three non-Qwen
# candidates share a transformers line.
#
# HOW TO INSTALL (in the CLONE, `$X`; never `source $X/bin/activate`, call `$X/bin/python` explicitly):
#   $X/bin/python -m pip freeze --exclude-editable | grep -E '^[A-Za-z0-9_.-]+==' \
#       | grep -viE '^(transformers|tokenizers|huggingface[-_]hub|safetensors|hf[-_]xet)==' > ~/arm2_x_constraints.txt
#   $X/bin/python -m pip install -c ~/arm2_x_constraints.txt --only-binary=:all: -r requirements-jetson-arm2-transformers-lt5.txt
# Everything EXCEPT the transformers-linked packages (transformers / tokenizers / huggingface_hub /
# safetensors / hf-xet, which the pin legitimately moves together) stays frozen, so numpy, torch and
# torchvision cannot drift; if the pin would need them to, pip refuses.

transformers>=4.56,<5

# InternVL2.5's trust_remote_code modeling files are BELIEVED to import these (recollection of the
# upstream repo, NOT verified this session). If InternVL2.5 fails to load with ModuleNotFoundError at
# the smoke stage, this is the first thing to check; harmless if unused.
timm
einops
