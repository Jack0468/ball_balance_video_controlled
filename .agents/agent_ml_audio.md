# Moved

This sub-agent prompt is now a native Claude Code subagent: **`.claude/agents/ml-audio.md`** — refreshed against current project state (this file had gone stale since 2026-08-11 and no longer reflected the real Phase 2 debugging progress).

Dataset/data-processing guidance previously implied here now lives in the **`dataset-integrity-check`** and **`data-processing-pipeline`** skills (`.claude/skills/`).
