# Moved

This orchestrator prompt has been superseded by native project instructions — see `CLAUDE.md` (repo root) for orchestration rules, MCP tool usage, and boundaries, and `.claude/agents/` for the four domain sub-agents it used to describe by hand.

(This file previously also carried a stale device reference — "Jetson Nano" — that had drifted from the corrected "Jetson AGX Orin 64GB p3730" elsewhere. `CLAUDE.md` is the single source of truth now, which is the point of the consolidation.)
