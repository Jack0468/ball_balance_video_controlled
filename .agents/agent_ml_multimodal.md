# Moved

This sub-agent prompt is now a native Claude Code subagent: **`.claude/agents/ml-multimodal.md`**.

Model-comparison methodology previously implied here now lives in the **`model-iteration-constraints`** skill; BC-dataset generation guidance lives in the **`data-processing-pipeline`** skill (`.claude/skills/`).
