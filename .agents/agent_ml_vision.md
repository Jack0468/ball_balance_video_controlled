# Moved

This sub-agent prompt is now a native Claude Code subagent: **`.claude/agents/ml-vision.md`** — fully rewritten against current project state (this file had gone stale since 2026-08-11, predating the ArUco homography pipeline, the Shared Backbone CNN, and nearly everything else currently real in the vision arm; it no longer reflected the project accurately).

Dataset/labeling-pipeline guidance previously implied here now lives in the **`data-processing-pipeline`**, **`data-pipeline-verification`**, and **`dataset-integrity-check`** skills (`.claude/skills/`).
