# Moved

This sub-agent prompt is now a native Claude Code subagent: **`.claude/agents/fpga.md`**.

The FPGA verification sequence and clocking/HLS pitfalls previously inlined here now live in the **`fpga-pipeline`** skill (`.claude/skills/fpga-pipeline/SKILL.md`).
